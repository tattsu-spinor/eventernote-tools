name: CI/CD
on:
  pull_request:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun run build
      - uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: ${{ github.ref_name != 'main' && format('versions upload --preview-alias={0}', github.ref_name)}}
          postCommands: |
            echo "### Deployment completed successfully" >> $GITHUB_STEP_SUMMARY
            echo "URL: ${{ github.ref_name != 'main' && format('https://{0}-eventernote-tools.tattsu.workers.dev', github.ref_name) || 'https://eventernote-tools.tattsu.workers.dev' }}" >> $GITHUB_STEP_SUMMARY
  biome:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun run biome:ci
